# Flask SECRET_KEY is required. Generate a unique 32-byte value per environment with
#   python -c "import secrets; print(secrets.token_hex(32))"
# Copy the generated string below for local development before starting the app.
SECRET_KEY=
